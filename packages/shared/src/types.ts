/** Preferred side to place a floating element (callout, tooltip) relative to
 *  its anchor. Shared so widgets speak the same vocabulary as `@floating-ui/dom`. */
export type Placement = "top" | "bottom" | "left" | "right";
